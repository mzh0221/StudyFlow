# StudyFlow V0 接口设计

## 1. 通用约定

- 基础路径：`/api/v1`。
- 身份认证：浏览器会话 Cookie；除登录接口外均需要登录。Cookie 使用 `HttpOnly`、`SameSite=Lax`，生产环境再启用 `Secure`。
- 数据格式：请求和响应为 `application/json`；文件导入使用 `multipart/form-data`。
- 时间格式：ISO 8601 且带时区，例如 `2026-09-16T09:00:00+08:00`。
- 成功响应直接返回资源或分页对象；创建资源返回 `201 Created`。

统一错误格式：

```json
{
  "code": "VALIDATION_ERROR",
  "message": "请求参数不合法",
  "fieldErrors": [
    { "field": "endAt", "message": "结束时间必须晚于开始时间" }
  ],
  "traceId": "a1b2c3d4"
}
```

常用错误码：`UNAUTHENTICATED`（401）、`FORBIDDEN`（403）、`NOT_FOUND`（404）、`VALIDATION_ERROR`（400）、`CONFLICT`（409）、`FILE_TOO_LARGE`（413）、`UNSUPPORTED_FILE_TYPE`（415）、`INTERNAL_ERROR`（500）。

## 2. 认证

### `POST /auth/login`

登录并创建会话。

请求：

```json
{ "username": "studyflow", "password": "example-password" }
```

成功响应 `200`：

```json
{ "id": 1, "username": "studyflow" }
```

失败时统一返回 `401 UNAUTHENTICATED`，不区分用户名和密码错误。

### `POST /auth/logout`

销毁当前会话。成功响应 `204 No Content`。

### `GET /auth/me`

返回当前登录用户；未登录返回 `401`。

## 3. ICS 导入与草稿确认

### `POST /imports/ics`

创建 ICS 导入任务。请求为 `multipart/form-data`，字段名为 `file`。

成功响应 `202`：

```json
{
  "id": 101,
  "importType": "ICS",
  "status": "PENDING",
  "originalFilename": "course.ics",
  "createdAt": "2026-09-16T10:00:00+08:00"
}
```

### `GET /imports/{importId}`

查询导入任务及统计信息。任务不属于当前用户时返回 `404`。

```json
{
  "id": 101,
  "status": "SUCCEEDED",
  "totalCount": 18,
  "successCount": 18,
  "failureReason": null,
  "createdAt": "2026-09-16T10:00:00+08:00",
  "finishedAt": "2026-09-16T10:00:02+08:00"
}
```

### `GET /imports/{importId}/drafts?status=PENDING&page=1&pageSize=50`

分页查询该导入任务的草稿。`status` 可省略。

```json
{
  "items": [
    {
      "id": 201,
      "title": "高等数学",
      "startAt": "2026-09-21T08:00:00+08:00",
      "endAt": "2026-09-21T09:40:00+08:00",
      "timezone": "Asia/Shanghai",
      "location": "教学楼 A101",
      "description": null,
      "recurrenceRule": "FREQ=WEEKLY;BYDAY=MO;UNTIL=20270118T015959Z",
      "excludedDates": ["2026-10-05T08:00:00+08:00"],
      "validationErrors": [],
      "duplicateOfScheduleId": null,
      "status": "PENDING",
      "version": 0
    }
  ],
  "page": 1,
  "pageSize": 50,
  "total": 18
}
```

### `PATCH /schedule-drafts/{draftId}`

编辑待确认草稿。仅 `PENDING` 草稿可编辑，必须携带 `version` 以避免覆盖并发修改。

请求：

```json
{
  "title": "高等数学",
  "startAt": "2026-09-21T08:00:00+08:00",
  "endAt": "2026-09-21T09:40:00+08:00",
  "timezone": "Asia/Shanghai",
  "location": "教学楼 A101",
  "description": "",
  "recurrenceRule": "FREQ=WEEKLY;BYDAY=MO;UNTIL=20270118T015959Z",
  "excludedDates": ["2026-10-05T08:00:00+08:00"],
  "version": 0
}
```

成功返回更新后的草稿。版本不一致返回 `409 CONFLICT`，前端应刷新草稿后提示用户重新修改。

### `POST /schedule-drafts/{draftId}/confirm`

确认单条草稿并创建正式日程。

请求：

```json
{ "version": 1 }
```

成功响应 `201`：

```json
{ "draftId": 201, "scheduleId": 301, "status": "CONFIRMED" }
```

字段不合法、疑似重复或已确认时返回 `409`，并给出可展示原因。重复请求不得产生第二条日程。

### `POST /imports/{importId}/drafts/confirm`

批量确认草稿。只确认传入且可确认的草稿；每项独立返回结果，避免一项失败导致全部回滚。

请求：

```json
{
  "items": [
    { "draftId": 201, "version": 1 },
    { "draftId": 202, "version": 0 }
  ]
}
```

成功响应 `200`：

```json
{
  "results": [
    { "draftId": 201, "success": true, "scheduleId": 301 },
    { "draftId": 202, "success": false, "code": "CONFLICT", "message": "与已有日程重复" }
  ]
}
```

## 4. 正式日程

### `GET /schedules?from=2026-09-01T00:00:00%2B08:00&to=2026-09-30T23:59:59%2B08:00`

查询日期区间内的正式日程。`from`、`to` 必填，最大查询范围为 92 天。每周重复日程在服务端展开为发生实例。

```json
{
  "items": [
    {
      "id": 301,
      "title": "高等数学",
      "startAt": "2026-09-21T08:00:00+08:00",
      "endAt": "2026-09-21T09:40:00+08:00",
      "location": "教学楼 A101",
      "description": null,
      "recurring": true,
      "sourceScheduleId": 301
    }
  ]
}
```

`sourceScheduleId` 指向规则日程；非重复日程与 `id` 相同。查询结果按 `startAt` 升序。

## 5. 学习任务

### `POST /study-tasks`

创建任务。

```json
{
  "title": "完成 Spring Boot 第三章练习",
  "description": "完成 1 到 10 题",
  "dueAt": "2026-09-20T22:00:00+08:00",
  "estimatedMinutes": 120,
  "priority": "HIGH"
}
```

成功响应 `201`，返回完整任务对象。

### `GET /study-tasks?status=TODO&page=1&pageSize=20`

查询任务。`status` 可选：`TODO`、`DONE`；省略时默认 `TODO`。响应采用与草稿列表相同的分页结构。

### `GET /study-tasks/{taskId}`

查询单项任务；不存在或不属于当前用户时返回 `404`。

### `PATCH /study-tasks/{taskId}`

更新未完成任务的标题、描述、截止时间、预计时长和优先级。字段校验失败返回 `400`。

### `POST /study-tasks/{taskId}/complete`

将任务标记完成。成功响应 `200`：

```json
{
  "id": 401,
  "status": "DONE",
  "completedAt": "2026-09-16T11:20:00+08:00"
}
```

重复调用保持幂等，仍返回当前完成状态。

### `DELETE /study-tasks/{taskId}`

删除任务。成功响应 `204 No Content`；不存在时返回 `404`。

## 6. 前后端联调顺序

1. 认证：登录、当前用户、退出和全局 `401` 跳转。
2. 学习任务：先完成完整 CRUD，提供可独立验证的页面。
3. ICS 导入：上传、轮询导入状态、展示草稿。
4. 草稿编辑与确认：处理字段错误、版本冲突和重复提示。
5. 日程查询：接入日历视图，验证重复规则和例外日期。

