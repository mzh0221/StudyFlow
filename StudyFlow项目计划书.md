# StudyFlow 项目计划书

## 项目概述

StudyFlow 是本地运行的个人学习任务编排系统。它将 ICS、自然语言、图片和 Excel/CSV 中的日程统一成可编辑的日程草稿；经用户确认后，系统计算空闲时间、生成学习计划并可靠发送提醒。AI 只负责提取、查询、解释与建议，永远不能直接写数据库。

项目的价值在于使用真实的个人课表和六级日程，逐步承接 Java 后端学习：并发、缓存、事务、消息队列、本地大模型与工程化都对应一个可演示功能。

## 范围与非目标

| 范围 | 第一版承诺 | 明确不做 |
|---|---|---|
| 账号 | 单本地账号、会话登录、BCrypt 密码 | 注册、多用户与协作 |
| 日程输入 | ICS、文本、PNG/JPG、Excel/CSV | PDF、Word、云日历同步 |
| 日程确认 | 草稿预览、编辑、冲突检测、去重、逐项或批量确认 | AI 自动写入正式日程 |
| AI | Qwen3 4B、本地 Ollama、SSE、受控工具调用 | RAG、外部付费模型依赖 |
| 运行 | Windows 本机 Docker Compose 演示 | 云端生产部署 |

## 技术与总体架构

技术基线为 Java 21、Spring Boot 4.1.1、MySQL 8、Redis 7、RocketMQ、Vue 3、Docker Compose、Ollama、Qwen3 4B、Tesseract OCR、Apache POI 和 Flyway。

Ollama 运行在 Windows 宿主机；后端容器通过 host.docker.internal 访问本地模型。MySQL、Redis、RocketMQ、MailHog、后端和前端由 Compose 启动。数据库变更均以 Flyway 迁移脚本维护。

```text
ICS / 文本 / 图片 / Excel / CSV
              ↓
格式校验 → 确定性解析或 OCR → Qwen3 结构化提取
              ↓
草稿校验 → 冲突检测与去重 → 用户确认
              ↓
正式日程 → 空闲时间计算 → 规则排程 → 计划确认
              ↓
Outbox → RocketMQ → 站内提醒 / 邮件提醒
```

## 核心模块

| 模块 | 关键职责 | 关键边界 |
|---|---|---|
| 认证 | 单账号登录、会话控制 | 初始账号与密码只由环境变量提供 |
| 导入 | 上传或接收文本，创建导入任务 | 限制 ICS/XLSX/CSV/PNG/JPG/JPEG，单文件 10MB |
| 解析 | ICS 重复规则；Excel/CSV 表头映射；OCR 文字提取 | 非 ICS 只支持一次性和每周重复日程 |
| 草稿 | 保存原文、置信度、字段错误、冲突和去重结果 | 未确认草稿不参与排程 |
| 排程 | 按截止时间、优先级、预计时长安排空闲块 | 每次 30–120 分钟；只生成建议 |
| 提醒 | Outbox、MQ、幂等消费、邮件重试 | 按业务唯一键去重，重复消息不重复通知 |
| AI | 查询日程、查询任务、解释冲突、生成建议 | 无任何直接写库工具；所有变更需用户确认 |

## 数据与接口

核心实体：本地账号、导入任务、源文件、日程草稿、正式日程、学习任务、计划块、提醒、Outbox 事件。

主要接口：登录；创建导入任务；查询导入状态；编辑和确认草稿；查询日程；任务 CRUD；生成和确认排程；查询提醒；AI SSE 对话。草稿包含标题、开始时间、结束时间、地点、重复规则、置信度、原始输入、字段错误和冲突标记。

模型输出必须经过 JSON、日期、时长、重复规则和权限校验。模型无响应或输出不合规时，保留原始输入与可编辑草稿，不影响 ICS/Excel 的确定性导入。

## 版本演进路线

| 版本 | 时间 | 学习承接 | 项目升级 | 面试亮点 |
|---|---|---|---|---|
| V0 个人学习 MVP | 2026年9–10月 | Spring Boot、MySQL、线程池 | 登录、任务、ICS 导入、课表查询 | 真实课表的产品建模 |
| V1 自动排程与缓存 | 2026年10–11月 | Redis、JVM、MyBatis、事务 | 空闲时间、规则排程、缓存、索引、乐观锁 | 缓存击穿、并发确认、慢 SQL |
| V1.5 可靠提醒与工程化 | 2026年11–12月 | RocketMQ、Docker、Linux | Outbox、幂等提醒、邮件、日志、Compose | 消息不丢与重复消费处理 |
| V2 多源日程理解 | 2027年寒假 | Spring AI、Ollama、OCR、Excel | 文本、截图、Excel/CSV、草稿确认 | 多源解析与 AI 安全边界 |
| V2.5 智能计划助手 | 2027年2–4月 | SSE、工具调用、限流、降级 | AI 查询、冲突解释、重排建议 | 模型为什么不能直接写库 |
| V3 可观测与性能 | 2027年4–6月 | Micrometer、压测、CI、测试 | 指标、链路日志、命中率、压测 | 性能问题定位与优化证据 |
| V4 求职展示版本 | 2027年暑假至秋招 | 系统设计、实习复盘 | 演示数据、架构图、故障演练、视频 | 20–30 分钟项目讲解 |

## 架构演进原则

- V0 至 V2.5 均保持模块化单体，不为了展示技术而强拆微服务。
- 用户、日程、任务、排程、提醒、AI 按模块隔离；只有提醒通过消息队列异步化。
- V3 只有在需要展示独立伸缩或故障隔离时才抽取通知服务；主分支仍保留可运行单体。
- 每个版本建立 Git 标签、更新日志、演示数据和数据库迁移说明，升级不破坏既有功能。

## 版本固定产物

每完成一个版本，同步更新 README、启动方式、环境变量、架构图、时序图、接口示例、测试记录、技术取舍和三道可被追问的面试题。V3 起追加压测报告、监控截图和一次故障演练记录。

## 测试与演示

| 场景 | 验收标准 |
|---|---|
| ICS | 正确处理重复、例外日期与导入失败 |
| 文本与图片 | 相对日期、OCR 错字、字段缺失进入可编辑草稿 |
| 表格 | Excel 日期格式、非标准表头、重复行得到可解释结果 |
| 排程 | 无空闲时间、超期任务、冲突日程和任务拆分有明确提示 |
| 并发与消息 | 重复确认只成功一次；重复消费不生成重复提醒 |
| AI 降级 | Ollama 不可用时保留原始输入，不写入错误日程 |
| 演示 | 截图课表导入 → 修正草稿 → 确认 → 创建任务 → 排程 → 提醒 → AI 解释，全程 5 分钟内完成 |

## 面试讲解顺序

先讲真实问题与用户流程，再讲日程标准化和排程规则；随后选择三个深挖点：Redis 缓存与击穿、Outbox 加幂等消费、多源输入的草稿确认与 AI 权限边界。最后展示压测或故障演练数据，并说明未选择微服务和 RAG 的原因。
