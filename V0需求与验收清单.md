# StudyFlow V0 需求与验收清单

## 1. 版本目标与边界

V0 是供单个本地用户使用的学习任务与课表管理 MVP。用户可以登录系统、导入 ICS 课表、查看已确认日程、管理学习任务；系统不在 V0 自动生成学习计划或发送提醒。

### 本版完成后应能演示的流程

1. 使用由环境变量配置的本地账号登录。
2. 上传一份 ICS 课表文件。
3. 预览解析出的日程草稿，修正明显错误后确认导入。
4. 在日历中查看已确认课表。
5. 新建、修改和完成一项学习任务。

### 本版明确不做

- 注册、多用户、找回密码和第三方登录。
- 文本、图片、Excel/CSV 导入与 AI 解析。
- 自动排程、Redis 缓存、消息队列、邮件或站内提醒。
- 云部署、移动端 App、云日历同步。

## 2. 角色与业务规则

| 角色 | 权限 |
|---|---|
| 本地用户 | 登录后管理自己的导入、日程草稿、日程和学习任务。 |
| 未登录访客 | 仅可访问登录接口与页面。 |

- 系统只支持一个由环境变量 `INITIAL_USERNAME`、`INITIAL_PASSWORD` 初始化的账号；密码仅以 BCrypt 哈希存储。
- 未确认的草稿不显示在正式日历中，也不参与未来排程。
- V0 的 ICS 导入需要支持单次事件、每周重复事件、结束日期和例外日期（`EXDATE`）。无法识别的字段保留在原始内容中，并给出导入警告或失败原因。
- 同一用户下，标题、开始时间、结束时间、地点和重复规则均相同的已确认日程视为重复，不能重复导入。
- 日程的结束时间必须晚于开始时间；学习任务预计时长必须为正整数分钟。

## 3. 功能需求与验收标准

### F-01 登录与会话

**用户故事：** 作为用户，我希望用本地账号登录，以保护自己的日程和任务数据。

| 编号 | 验收标准 |
|---|---|
| F-01-01 | 首次启动时，系统用环境变量创建或更新初始账号；数据库中不保存明文密码。 |
| F-01-02 | 输入正确用户名和密码后，返回有效登录会话；刷新页面后会话仍有效。 |
| F-01-03 | 用户名或密码错误时，返回统一的“用户名或密码错误”提示，不能泄露账号是否存在。 |
| F-01-04 | 未登录调用受保护接口时，返回 `401 Unauthorized`。 |
| F-01-05 | 用户主动退出后，会话失效，访问受保护页面会回到登录页。 |

### F-02 创建并解析 ICS 导入任务

**用户故事：** 作为用户，我希望上传 ICS 课表，以减少手工录入日程。

| 编号 | 验收标准 |
|---|---|
| F-02-01 | 仅接受 `.ics` 文件，单文件不超过 10 MB；类型或大小不符合时，返回明确错误。 |
| F-02-02 | 上传成功后创建导入任务，前端可查询其状态：`PENDING`、`PARSING`、`SUCCEEDED`、`FAILED`。 |
| F-02-03 | 正常 ICS 中的标题、开始时间、结束时间、地点、描述、每周重复规则、结束日期和例外日期能被解析。 |
| F-02-04 | 解析失败时，任务标记为 `FAILED` 并记录可展示的失败原因；既有正式日程不受影响。 |
| F-02-05 | 同一文件可再次上传，但每次均创建独立导入任务，便于追溯。 |

### F-03 草稿预览、编辑与确认

**用户故事：** 作为用户，我希望在写入课表前检查并修改导入结果。

| 编号 | 验收标准 |
|---|---|
| F-03-01 | 解析成功后，每条候选日程以草稿形式展示标题、时间、地点、重复信息、警告和是否疑似重复。 |
| F-03-02 | 用户可编辑草稿的标题、开始时间、结束时间、地点、描述和重复规则；保存时校验结束时间晚于开始时间。 |
| F-03-03 | 用户可逐条确认或批量确认无错误草稿；确认成功后生成正式日程，并记录确认时间。 |
| F-03-04 | 标记为重复或字段不合法的草稿不能直接确认；页面须说明原因，用户修正后可再次确认。 |
| F-03-05 | 对同一草稿重复提交确认，只会创建一条正式日程。 |

### F-04 查询正式日程

**用户故事：** 作为用户，我希望按日期查看自己的课表。

| 编号 | 验收标准 |
|---|---|
| F-04-01 | 用户可按开始日期和结束日期查询已确认日程。 |
| F-04-02 | 单次日程在其时间范围内返回；每周重复日程按查询区间展开为实际发生时间。 |
| F-04-03 | `EXDATE` 指定的日期不返回；查询结果按开始时间升序排列。 |
| F-04-04 | 用户只能查询自己的日程；V0 即使只有一个账号，也必须保留该权限边界。 |

### F-05 学习任务管理

**用户故事：** 作为用户，我希望记录待完成的学习事项，为后续自动排程提供输入。

| 编号 | 验收标准 |
|---|---|
| F-05-01 | 用户可创建学习任务：标题必填；截止时间、预计时长、优先级、备注可选。 |
| F-05-02 | 优先级只允许 `LOW`、`MEDIUM`、`HIGH`；预计时长填写时必须为 1–1,440 分钟。 |
| F-05-03 | 用户可编辑未完成任务，或将任务标记为完成；完成后记录完成时间。 |
| F-05-04 | 用户可按状态筛选任务，默认显示未完成任务，并按优先级、截止时间和创建时间排序。 |
| F-05-05 | 用户可删除任务；删除后列表和详情均不可再访问。 |

## 4. 页面与流程最小集

| 页面 | 必需内容 | 主要操作 |
|---|---|---|
| 登录页 | 用户名、密码、登录错误提示 | 登录 |
| 日程页 | 日期范围、日历/列表、导入入口 | 查询日程、进入导入 |
| 导入详情页 | 导入状态、草稿列表、字段警告 | 编辑、确认单条、批量确认 |
| 任务页 | 任务列表、状态筛选、新建入口 | 新建、编辑、完成、删除 |

## 5. 完成定义（Definition of Done）

某项 V0 功能完成，至少满足以下条件：

- 后端接口具备请求校验、权限校验和可理解的错误响应。
- 前端具备正常、空数据、加载和失败状态。
- 核心业务规则有自动化测试：登录失败、ICS 重复/例外日期、草稿重复确认、任务状态变更。
- 数据库结构通过 Flyway 迁移创建；没有依赖手动改表。
- README 更新了启动步骤和本功能的最短演示路径。

